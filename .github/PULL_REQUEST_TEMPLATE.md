## Summary
<!-- 1–3 sentences describing what this PR changes and why. -->

## Test plan
- [ ] `python -m unittest discover -s tests` is green locally
- [ ] If UI changed: tested the affected screen in-game
- [ ] If save format changed: bumped `version` in `save_system.py` and added a migration path
- [ ] If a new control binding was added: confirmed `TestMultiplayerKeyConflicts` still passes

## Screenshots / GIFs
<!-- Optional but very helpful for visual changes. -->

## Notes for reviewer
<!-- Anything not obvious from the diff — design decisions, follow-ups deferred, risks. -->
